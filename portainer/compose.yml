
services:
  portainer:
    image: portainer/portainer-ce:latest
    container_name: portainer
    restart: unless-stopped

    command: -H unix:///var/run/docker.sock

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./data:/data

    networks:
      - proxy

    labels:
      - traefik.enable=true
      - traefik.http.routers.cp.rule=Host(`cp.${DOMAIN}`)
      - traefik.http.routers.cp.entrypoints=websecure
      - traefik.http.routers.cp.tls.certresolver=letsencrypt
      - traefik.http.services.cp.loadbalancer.server.port=9000

networks:
  proxy:
    external: true
